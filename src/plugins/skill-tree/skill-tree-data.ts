/**
 * Skill Tree — Predefined Skill Nodes
 *
 * Re-export from split modules for backward compatibility.
 * Actual data lives in ./data/ (one file per category).
 */

export { CATEGORIES, SKILL_TREE_NODES } from "./data";
